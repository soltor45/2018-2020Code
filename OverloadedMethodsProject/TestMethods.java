import java.util.Scanner;

public class TestMethods
{
    private int value;
    
    Scanner sc = new Scanner(System.in);
    
    System.out.println("Enter an integer: ");
    value = sc.nextInt();    if (value ==0)
    {
        calcData();
    }
    else if(value % 2==0)
    {
        calcData(value)
    }
    else 
    {
        calcData(value)
    }
    /**
     * Constructor for objects of class TestMethods
     */
//    public TestMethods()
 //   {
        // initialise instance variables
 //       x = 0;
 //   }
    public string calcData()
    {
        string hello = "hello";
        return hello;
    }
    public int calcData(int value)
    {
        int even2 = even + 2;
        int sum = even2 + even;
        return sum;
    }
    public double calcData(int value)
    {
        double oddby7 = odd/7;
        return oddby7;
    }
}
